const Logo = () => {
  return (
    <div>
      <span className="inline-block font-bold bg-gradient-to-r from-blue-500 to-pink-500 bg-clip-text text-transparent">
        MagicBooks
      </span>
      <span className="inline-block text-violet-500">.</span>
    </div>
  );
};

export default Logo;
